package com.example.tasky.models.entities.subtask

import com.example.tasky.models.TasksDatabase

class SubtasksRepository(private val db: TasksDatabase) {
//    suspend fun insert(subtask: Subtask) = db.getSubtasksDao().insert(subtask)
//
//    suspend fun delete(subtask: Subtask) = db.getSubtasksDao().delete(subtask)
//
//    fun getAllSubtasks(taskID: Int) = db.getSubtasksDao().getAllSubtasks(taskID)
}